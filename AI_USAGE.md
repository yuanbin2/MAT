# AI_USAGE.md

如实记录本作业中使用 AI 的方式、AI 曾给出的错误诊断、以及哪些判断是自己做的。

## 1. 用了哪些 AI 工具、怎么拆任务

全程使用 WorkBuddy（本次会话的编码助手）完成开发。拆任务的方式是把整份作业按"数据流层次"切成可独立验证的小块，而不是让 AI 一口气重写：

1. 先读 `docs/ASSIGNMENT.md`、`docs/API_CONTRACT.md`、`eval/README.md`、公开题库、`starter/HANDOVER.md` 与现有实现；
2. 固定基线（跑公开评测、用 worktree 复现原始 starter 得分）；
3. 按"装载/切块 → 分词/召回 → 排序/版本 → 路由/取证 → 追问/安全"逐层定位与修复，每层先跑最小复现、再改、再跑针对性评测；
4. 每一层修完跑一次 `--only retrieval` 或相关类别，记录分数变化，避免上游缺陷掩盖下游。

一个真实 prompt 例子（用于定位检索为什么大量 0 分）：

> 帮我拉取并逐题查询 R01–R15 的 /api/retrieve 结果，输出每题 top-5 的 doc_id/chunk_id/score，以及 gold 文档是否命中。我要建立缺陷台账，先定位证据再动代码。

## 2. AI 给过哪些错误的诊断或修复，怎么发现的

- **错误诊断 1：把"趋势图切换日期后不显示"归因于 ECharts 动画未完成。** 我最初怀疑是 `--virtual-time-budget` 下动画没画完。实际根因是组件里 `watch` 默认 pre-flush，在 `v-if` 重挂载容器前就调了 `render()`，图表画在了已脱离文档的旧 DOM 上。靠真实浏览器交互复现（切日期点查询后 canvas 丢失）才确认，而不是靠读代码臆测。
- **错误诊断 2：验证脚本里的 playwright 路由 glob 静默失效。** 写"接口失败重试"的验证脚本时，第一轮误报"指标失败没有保留旧结果"。排查后发现是 playwright 的 `page.route` glob 里 `?` 是通配符，带 query string 的请求（`/api/metrics/summary?start=...`）没被拦截。这**不是产品 bug，是验证脚本的 bug**——修成 `**/api/metrics/summary*` 后五态才全部通过。教训：验证脚本失效和被验证对象出错要分清，不能把脚本的问题记到产品头上。
- **错误诊断 3：puppeteer-core 在本机无法启动 Edge（Code 0 / SIGTERM）。** 反复重试后确认是环境问题（火绒扫描 + 沙箱进程限制），不是代码问题，最终改用 playwright-core + 显式 `executablePath` 才稳定。这条没有误导最终结论，但浪费了几轮，记下来提醒自己"环境类报错先隔离环境，别在代码里打转"。

## 3. 哪些判断是自己做的，为什么不交给 AI

- **口径取舍**：清洗按 KB-001 v3 的六类剔除顺序、退款计入净营业额、客单价除以有效订单数等，都是我对照手册逐条确认的，没有让 AI"凭印象"实现。理由：这些是本次作业的核心考点，AI 的记忆可能混入 v2 旧口径，必须回到手册原文核对。
- **根因定位**：每个缺陷都要求"先有证据再改"——用最小实验（直接调 `tokenize`、构造 301 字文档、打印 `meta.get("status")`）确认根因后才动手。AI 容易给"优化检索"这类笼统建议，但我要的是"文件 + 行号 + 会红的测试"。
- **黄金值对账**：清洗后 `valid_sales_rows` 必须等于 18290、6 月净营业额必须 156757.00，这些黄金值是我用独立脚本先算出来、再与接口返回逐项核对的，没让 AI 直接"写死答案"。
- **拒绝硬编码**：明确要求不针对题号、文档编号、原始数据值做特判；索引必须能跟着知识库变。换数据重建的验证（新增 KB-099、改一条金额）是我自己设计的，确认了"文档新增 → kb_docs 变化、引用仍逐字、口径仍正确"。
- **提交拆分与红绿顺序**：分层提交（装载/切块/分词 → 召回/排序/版本 → 路由/追问/安全），每个 commit 对应一类根因和一组回归测试。这个节奏是我定的，AI 只负责按我给的边界改代码。
