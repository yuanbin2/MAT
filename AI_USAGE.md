# AI_USAGE.md

如实记录本作业中使用 AI 的方式、AI 曾给出的错误诊断、以及哪些判断是自己做的。

## 1. 用了哪些 AI 工具、怎么拆任务

全程使用 WorkBuddy（本次会话的编码助手）完成开发。拆任务的方式是把整份作业按"数据流层次"切成可独立验证的小块，而不是让 AI 一口气重写：

1. 先读 `docs/ASSIGNMENT.md`、`docs/API_CONTRACT.md`、`eval/README.md`、公开题库、`starter/HANDOVER.md` 与现有实现；
2. 固定基线（跑公开评测、用 worktree 复现原始 starter 得分）；
3. 按"装载/切块 → 分词/召回 → 排序/版本 → 路由/取证 → 追问/安全"逐层定位与修复，每层先跑最小复现、再改、再跑针对性评测；
4. 每一层修完跑一次 `--only retrieval` 或相关类别，记录分数变化，避免上游缺陷掩盖下游。

一个真实 prompt 例子（用于定位检索为什么大量 0 分）：

> 帮我拉取并逐题查询 R01–R15 的 /api/retrieve 结果，输出每题 top-5 的 doc_id/chunk_id/score，以及 gold 文档是否命中。我要建立缺陷台账，先定位证据再动代码。

## 2. AI 给过哪些错误的诊断或修复，怎么发现的

- **错误诊断 1：把"趋势图切换日期后不显示"归因于 ECharts 动画未完成。** 我最初怀疑是 `--virtual-time-budget` 下动画没画完。实际根因是组件里 `watch` 默认 pre-flush，在 `v-if` 重挂载容器前就调了 `render()`，图表画在了已脱离文档的旧 DOM 上。靠真实浏览器交互复现（切日期点查询后 canvas 丢失）才确认，而不是靠读代码臆测。
- **错误诊断 2：验证脚本里的 playwright 路由 glob 静默失效。** 写"接口失败重试"的验证脚本时，第一轮误报"指标失败没有保留旧结果"。排查后发现是 playwright 的 `page.route` glob 里 `?` 是通配符，带 query string 的请求（`/api/metrics/summary?start=...`）没被拦截。这**不是产品 bug，是验证脚本的 bug**——修成 `**/api/metrics/summary*` 后五态才全部通过。教训：验证脚本失效和被验证对象出错要分清，不能把脚本的问题记到产品头上。
- **错误诊断 3：puppeteer-core 在本机无法启动 Edge（Code 0 / SIGTERM）。** 反复重试后确认是环境问题（火绒扫描 + 沙箱进程限制），不是代码问题，最终改用 playwright-core + 显式 `executablePath` 才稳定。这条没有误导最终结论，但浪费了几轮，记下来提醒自己"环境类报错先隔离环境，别在代码里打转"。

## 3. 哪些判断是自己做的，为什么不交给 AI

- **口径取舍**：清洗按 KB-001 v3 的六类剔除顺序、退款计入净营业额、客单价除以有效订单数等，都是我对照手册逐条确认的，没有让 AI"凭印象"实现。理由：这些是本次作业的核心考点，AI 的记忆可能混入 v2 旧口径，必须回到手册原文核对。
- **根因定位**：每个缺陷都要求"先有证据再改"——用最小实验（直接调 `tokenize`、构造 301 字文档、打印 `meta.get("status")`）确认根因后才动手。AI 容易给"优化检索"这类笼统建议，但我要的是"文件 + 行号 + 会红的测试"。
- **黄金值对账**：清洗后 `valid_sales_rows` 必须等于 18290、6 月净营业额必须 156757.00，这些黄金值是我用独立脚本先算出来、再与接口返回逐项核对的，没让 AI 直接"写死答案"。
- **拒绝硬编码**：明确要求不针对题号、文档编号、原始数据值做特判；索引必须能跟着知识库变。换数据重建的验证（新增 KB-099、改一条金额）是我自己设计的，确认了"文档新增 → kb_docs 变化、引用仍逐字、口径仍正确"。
- **提交拆分与红绿顺序**：分层提交（装载/切块/分词 → 召回/排序/版本 → 路由/追问/安全），每个 commit 对应一类根因和一组回归测试。这个节奏是我定的，AI 只负责按我给的边界改代码。

## 4. 第三关前置加固这一轮（只读边界 / live 取证 / trace）

同样是"先红后绿"，并**实际检出修复前的 commit 用 worktree 复现**，而不是只声称"修复前会失败"：

1. 先读 `tools.py`、`cleaning.py`、`live.py`、`llm.py`、`loader.py`、`eval/run_eval.py`（含评测端 `sql_problems` 的判定口径），确定"只读"的验收标准与契约数值上限；
2. 用一个临时库副本做**行为实验**，确认漏洞真实存在（见下面"错误诊断 4"）；
3. 为每个缺陷写会在旧实现上失败的检查，`git worktree` 检出 `2a26866` / `a794359^` / `8f8054a^` 跑出**真实红输出**（例如 DELETE 把行数从 2 清到 0、`ATTACH` 被放行、结果 11047 字节、命中 `doc_id=KB-013` 配 `chunk_id=KB-002#3`、旧 `_context` 返回 201 字、数字只在问题里也被放行）；
4. 再改实现、把同样的检查在新代码上跑成绿，并落成 `tests/` 下的常驻回归测试。

一个真实 prompt 例子（用于定位只读边界）：

> 优先保证 DataTools 使用真正只读的 SQLite 连接；run_sql 只接受单条、实际读取表的查询，禁止任何写入及数据库附加操作。用临时数据库副本测试 DELETE、UPDATE、DROP 均失败且行数不变。

## 5. 这一轮 AI 给过的错误判断，以及我怎么发现的

- **错误诊断 4：以为"把连接设成 `mode=ro` 就够了"。** 这是最容易想当然的地方。我坚持先做实验，结果实测发现：`mode=ro` 能挡住 DELETE/UPDATE/DROP/CREATE，但**仍然放行 `ATTACH DATABASE`**，连 `PRAGMA query_only=ON` 也挡不住——也就是说，光靠只读连接，"附加别的库"这条库级操作是漏的。于是补了一层 `sqlguard.check_readonly_sql` 词法闸门。**如果只信"只读连接"这个直觉、不做这个实验，这个洞会一直留着。** 这一条直接写进了 `sqlguard.py` 的文档注释与 `DEBUG_LOG.md`。
- **错误判断 5：把"数字白名单"当成越宽松越安全。** 初版把整篇文档、问题原文、工具参数里的数字都并进白名单，看似"不容易误判"，实际是给模型蒙数字留了口子。用 worktree 复现出旧实现的两处真实放行（数字只在问题里、数字只在整篇文档里都被接受）后，才收紧为"只认本轮证据结果 + 本轮检索片段"。教训：**宽松的白名单不是保守，是漏洞。**
- **验证脚本与产品要分清（再次）**：写 live 数字校验的红测时，第一版用例选了"问题里没有那个数字"的样例，结果旧实现也返回兜底（GREEN），没测出问题。改成"数字确实出现在问题里"才复现出放行。**用例没选对，会得出"旧代码没问题"的错误结论**——所以红测本身也要验证它真的红。
- **错误判断 6：用 `str.isalnum()` 判断"数字是否紧贴编号"。** 这条是现在最值得记的：`"是".isalnum()` 在 Python 里是 `True`（汉字算 alnum），所以用 `cleaned[start-1].isalnum()` 排除 `S02`/`P06`/`KB-013` 时，会把"**是**9999999元"里的数字一起删掉——中文紧贴的数字**根本没进校验**，等于校验被静默跳过。红测直接暴露了这点（`_numbers_in("净营业额是9999999元")` 返回 `[]`）。修法是紧贴判断只按 ASCII 字符集，中文照常识别、编号仍被排除。教训：**别用 Python 的"听起来差不多"的内建判断（isalnum/isalpha）做字符分类**，它对 Unicode 的覆盖范围和直觉不一致。

## 6. 这一轮刻意没交给 AI 的判断

- **只读的第二道防线要不要做**：结论是"要做"，依据是实验数据（`mode=ro` 放行 ATTACH），不是 AI 的建议。
- **数字白名单的来源边界**：定为"仅本轮证据结果 + 本轮检索片段"，把契约的"数字必须来自实际查询"翻译成可执行规则，并接受"模型若写出无出处数字就回退模板回答"这一代价。
- **红测证据必须来自真实旧提交**：所有"修复前为红"的说法都用 `git worktree` 实测过，没有沿用"间接覆盖"这种含糊表述——`DEBUG_LOG.md` 里 D3/D4/D5/D6/D7/D8 的红测证据已按实测输出重写。

## 7. 第三关（混合问答 + 前端工作区）这一轮

任务被拆成「安全与取证 → live 编排 → 前端 → 验证和文档」四段提交，每段独立可验证：

- **live 编排**：`search_kb` 从"模型给 query 就裸查"改为复用 `/api/retrieve` 同一套 retriever 并注入
  Plan 的 as_of/historical/store_id/year/window——这是"历史版本由规划器决定，不靠模型猜"的直接落地。
  数据库工具参数与 Plan 的一致性校验（查错月份/门店不进证据）是我把"证据必须可溯"这条契约要求
  翻译成可执行规则的结果，AI 最初只做了参数**类型**校验，没有做**与问题的一致性**校验。
- **scope 校验的代价我接受了**：预检的 `normal`/`slow` 场景因此只产出 refusal，P14 变成"未检查"。
  我没有为了凑一个 PASS 而放宽 scope 校验（那等于放弃"查错区间不作为证据"），而是按规范在
  `LLM_SETUP.md` 第 7 节如实解释"未检查"的原因——规范明文说"未检查"不算失败。
- **前端**：在现有 Vue3+TS 架构内加一个 `ChatAssistant.vue`，不引入新框架；对话范围不继承看板筛选
  （避免"未提示就偷偷影响"）；不用 v-html；不暴露 reasoning_content。
- **红测诚实性**：D21（业务表白名单）我跑了 worktree 红测；D22（文件路径）是叠在前两层之上的
  保险，**没有**单独红测，就在 DEBUG_LOG 里如实写"belt-and-suspenders、无单独红测"，不冒充缺陷修复。

一个真实 prompt 例子（第三关的拆分指令，节选）：

> 按“安全与取证 → live 编排 → 前端 → 验证和文档”分次提交，不要只留一个 finish commit。
> ……若某项按规范是“未检查”，在报告中解释。

## 8. 第三关用到的验证方法（可复用）

- **预检驱动**：`eval/llm_gateway.py preflight` 需要"起假模型 → 用其环境变量重启服务 → 回车"，
  交互式流程写不进脚本；用 `run_preflight(..., ready_hook=..., no_wait=True)` 在一个驱动脚本里
  完成"假模型就绪 → 自动拉起带环境变量的后端 → 等健康检查通过 → 逐项检查"，全程无人工介入。
- **换数据/文档重建验证**：在 `git worktree` 临时副本里改 `data/pos.db` 一条金额、新增一篇 KB-099，
  用**独立的 VAR_DIR** 重建（`index_path` 不随 VAR_DIR 变，所以必须用 worktree 隔离，
  否则会污染主工作区的索引缓存），断言指标跟着变、kb_docs 变、口径不变、新文档可检索且引用逐字。

## 9. 第四关（可调试性）这一轮

按“trace 补全 → 调试面板 → 回归 CI → 自拟题与演练 → 文档”分次提交，每次提交里写了验证结果。

- **trace 只记真实发生的事**：mock 路径过去只有规划和检索、工具调用没有步骤；这轮把 `_call` 也接了 trace，
  并且 live 的工具步骤从“只有参数”补成“参数 + 结果 + 耗时 + status + 是否采纳 + 拒绝原因”。
  没有测量到的耗时写 `None`（面板显示“未记录”），**没有用 0 冒充**。
- **并发隔离**：明确不用任何模块级 `current_trace`；每次请求新建 `Trace` 并由调用链以参数传下去。
  加了一个单测断言两个 `Trace` 互不污染（`test_trace_objects_do_not_share_state`）。
- **红测诚实性（再次）**：回归门禁的“有效性”是我**实际把 C01 改低**跑出来的（退出码 1 + 具体题号 + trace_id），
  不是嘴上说“会失败”。
- **拒绝把“未检查”写成“通过”**：预检 P14 仍然是“未检查”，`EVAL_REPORT.md`/`LLM_SETUP.md` 都如实写明原因，
  没有为了好看改成 PASS。
- **一次真机验证的收获**：用 StepFun `step-5-preview` 实测时发现模型把周报估算当证据引用，
  修了 `live._citations`（`4e7231e`）。这条**不是** DeepSeek 的分数，也没有当成评审配置，
  只在 `EVAL_REPORT.md` 里作为过程记录标注清楚。
- **一处诚实的限制**：自拟题里我先写了改写句「点了外卖想退款，有没有时限」，结果 mock（纯 BM25）
  拒答了——**回答里“改写稳健性”有词汇边界**。我没有把它放宽成一条永远不会红的检查，
  而是换成能被正确回答的改写句，并在提交信息与文档里写下这个边界。

## 10. 第四关用到的验证方法（可复用）

- **就地复现回归**：`python eval/check_regression.py --report report.json` 拿 trace_id；
  改一道题的成绩再跑一次，确认门禁真的会红（否则门禁等于没有）。
- **演练脚本必须自清理**：`eval/drill_new_doc.py` 全程在 `tempfile.mkdtemp()` 里做，
  `finally` 里停服务 + 删目录；跑一次就能确认“改知识库 → 重建 → 重启 → 检索/回答/引用/trace”整条链路。
- **重建前必须先停服务**：`build_clean_db` 会重写 `var/clean.db`，而运行中的服务持有它
  （Windows 会锁文件，报 `WinError 32`）。演练脚本第一次就是这么失败的，停下来再重建才过——写进了手册。

## 11. 把模型配置放进 `.env` 这一轮

- **任务拆分**：先确认"文档说的 .env 到底能不能用"，再补 `config.py` 的读取，最后把 mock/live 的分界
  在测试、CI、演练三处都钉死。
- **一个被文档掩盖的缺口**：README 与 `LLM_SETUP.md` 早就写着"把 Key 放 `.env`"，而 `config.py` 只读
  `os.environ`、项目也没装 `python-dotenv`。**文档写了不等于实现做了**——先起服务看 `/api/health` 的
  `llm_mode`，一眼就验证了没生效（D28）。
- **AI 给过的错误诊断（我自己的第一版修复就是错的）**：全量测试突然变成 `2 failed, 183 passed`，
  两个失败用例**单独跑都通过**。AI 的第一反应是"新测试把 `LLM_API_KEY` 泄漏了"，于是给夹具加了
  `monkeypatch.delenv(...)` ——**改完仍然失败**。真正管用的是打表：写了个临时 pytest 插件在每个用例
  `pytest_runtest_setup` 打印环境变量，看到泄漏从哪个用例开始；再去读
  `_pytest.monkeypatch.MonkeyPatch.delitem` 的源码，才明白 **`delenv` 对本来不存在的键根本不记录撤销动作**，
  所以那条路永远兜不住直接写 `os.environ` 的代码。最后改成手写快照还原，并加了一对守门用例。
  **"改完还红"是这次最关键的信号——它否掉了凭直觉的第一个假设。**
- **刻意没交给 AI 的判断**：`.env` 与真实环境变量的优先级定为"真实环境变量优先"。理由不是习惯，
  而是项目的既有机制：预检会用注入的 `LLM_*` 起服务、演练要强制 mock——如果 `.env` 能覆盖它们，
  这两条链路就会被本地配置悄悄带偏。`ENV_FILE=` 这个总开关同理，是为了让"关掉 .env"变成一件
  能写进 CI 和脚本的确定的事。
- **验证方法**：`ENV_FILE=` 起服务看 `llm_mode`；把 `.env` 放到临时路径用 `ENV_FILE=/path/.env` 指过去；
  以及"`.env` 被忽略 / `.env.example` 可入库"用 `git check-ignore` 写成守门测试——避免哪天 Key 被误提交。

## 12. 第四关验收问题修复这一轮

- **任务拆分**：按"模型启动配置 → 演练隔离 → trace 语义 → 现场操作"四段推进，每段都先拿红证、再改、
  再跑回归。四段各自独立提交，便于回溯是哪一段带来的变化。
- **AI 的真实作用与我的把关**：这一轮 AI 主要干的是"把假设变成可执行实验"——
  例如 `export FOO` 的空串问题，它在最小 Makefile 里跑出 `ENV_FILE=''` 才算证实；
  trace 采纳语义的改动，它先在 `live.py` 里改回旧写法跑测试，看到 4 例失败才认账。
  **没有红证的"根因"我一律不接受**，这一轮四个问题都是这么定下来的。
- **一个反直觉的判断**：`--only doc` 的局部报告不能直接跟全量基线比。顺着"不改评测标准"的思路，
  我没有放宽判定（那会让门禁失去意义），而是给判定脚本加了语义明确的 `--subset`：
  只比局部出现过的题、声明总分与分类分未比较、基线里没有的题号直接报错。
  实测同一份报告：加 `--subset` 通过、不加报 57 处假回归——把两者差异摆在文档里，而不是靠口头约定。
- **本机没有 make 这件事**：README 一直写着"没 make 就用等价命令"，所以 Make 那条 bug 从来没被真正验证过。
  这一轮按要求装了 GNU Make 4.4.1 便携版（放在项目外、不入库），测试用 `MAKE_BIN` 指过去，
  CI 的 ubuntu 自带 make 所以不设也能跑。**"文档里写了等价命令"不等于"make 真能用"**。
- **演练隔离的取舍**：索引默认写在仓库里跟踪的 `.cache/index.json`，这是"clone 下来就能直接起服务"
  换来的便利；代价是任何用临时知识库重建的动作都会踩到它。加 `INDEX_PATH` 之后，
  便利保留、隔离靠一个环境变量解决，并加了"跟踪索引必须等于真实知识库"的守门测试防止再犯。


